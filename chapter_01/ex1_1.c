#include <stdio.h>

/* 
 * starts program execution. 
 */
 int main(void) {
 	// omit the closing parenthese ')' as well as the ';'
 	// to see what error will be displayed.
	printf("hello, world\n" //);
	return 0;
}
